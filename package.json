{
  "name": "landlord-app-guide",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy:pages": "node build.js && cmd /c \"npx wrangler pages deploy frontend/dist --project-name site-blogs --commit-dirty=true\"",
    "deploy:worker": "node build.js && cmd /c \"cd worker && npx wrangler deploy\"",
    "deploy": "wrangler deploy",
    "start": "npx serve frontend/dist",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "wrangler": "^4.68.1"
  }
}
